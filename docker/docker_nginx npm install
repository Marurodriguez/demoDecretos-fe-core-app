FROM alpine as build

RUN apk add --update nodejs npm nginx

COPY ./docker/nginx.conf  /etc/nginx/conf.d/default.conf

WORKDIR /data
#COPY . .
#RUN npm install -G @angular/cli
#RUN npm install
#RUN npm run ng build
#RUN cp dist/consultasDigitalizaciones /usr/share/nginx/html
COPY ./dist/consultasDigitalizaciones /usr/share/nginx/html

CMD ["/bin/bash", "-c", "chmod 755 /usr/share/nginx/html/* && nginx -g 'daemon off;'"]
